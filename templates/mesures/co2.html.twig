{% extends 'graphique.html.twig' %}

{% block body %}

        <form role="form" method="get">
            <div class="form-group">
                <label class="form-label mt-4" for="InputDate">Date de Début</label>
                <input id="DateMin" class="form-control" name="DateMin" type="date" value="form.min"
                       placeholder="Enter Date">
                <label class="form-label mt-4" for="InputDate">Date de Fin</label>
                <input id="DateMax" class="form-control" name="DateMax" type="date" value="form.max"
                       placeholder="Enter Date">
            </div>
            <div class="container text-center">
                <div class="row align-items-start">
                    <div class="col">
                    </div>
                    <div class="col">
                        <input type="submit" name="Submit" class="btn btn-success btn-lg" value="Afficher">
                    </div>
                    <div class="col">
                        <a style="text-decoration:none" class="btn btn-primary btn-lg" href="{{ path('co2') }}">Reset</a>

                    </div>
                    <div class="col">
                    </div>
                </div>
            </div>

        </form>
    {% if(d!= 0 ) %}
        <div class="chart-card">
            <div class="card-body">
                {{ render_chart(chart4) }}
            </div>
            <div class="card-footer">
                <div class="footer">
                    <a href="{{ path('courbe') }}" class="btn btn-secondary">
                        <span></span> Retour
                    </a>
                </div>
            </div>
        </div>

    {% endif %}

{% endblock %}